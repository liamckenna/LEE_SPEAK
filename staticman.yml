apiVersion: v3
branch: master
format: json

mountPoints:
  comments:
    path: data/comments/{options[path]}
    fileName: "{options[slug]}-{timestamp}"

fields:
  name:
    type: string
    required: true
  comment:
    type: text
    required: true
